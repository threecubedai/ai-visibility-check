name: "AI Visibility Check"
description: "Check whether a URL is ready to be cited by AI engines, and optionally fail the build below a score."
branding:
  icon: "search"
  color: "orange"
inputs:
  url:
    description: "The URL to check."
    required: true
  min-score:
    description: "Fail the job if the score is below this value (0-100). Default 0 (never fail)."
    required: false
    default: "0"
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: "20"
    # Runs the tool straight from the public repo, no npm publish required.
    - shell: bash
      run: npx --yes github:threecubedai/ai-visibility-check "${{ inputs.url }}" --min=${{ inputs.min-score }}
